# Example bellwether config. Copy to `config.toml` and
# customize. `config.toml` is gitignored.
#
# Run with:  cargo run -p bellwether-web -- --config config.toml

[weather]
# Open-Meteo is free and needs no API key. `model`
# supports any of Open-Meteo's forecast models — see
# https://open-meteo.com/en/docs — with `icon_eu`
# (Slovenia-optimised) as the default.
provider = "open_meteo"
# Ljubljana: change to your point of interest.
lat = 46.05
lon = 14.51

[weather.open_meteo]
model = "icon_eu"

[trmnl]
mode = "byos"
# URL the TRMNL device uses to fetch rendered BMPs.
# Localhost works for local poking; change to the
# LAN-visible hostname of the machine running the
# server before pointing a real device at it.
public_image_base = "http://localhost:3100/images"
# 900 s = 15 min. Fine for a TRMNL dashboard and a
# comfortable cadence for the e-ink refresh.
default_refresh_rate_s = 900

[render]
width = 800
height = 480
bit_depth = 1
timezone = "Europe/Ljubljana"
